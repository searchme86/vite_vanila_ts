const listMenu = document.querySelector('.list_menu');
const listItems = document.querySelectorAll('.list-item_menu .text_menu');
const menuContents = document.querySelectorAll('.menu_content');
const header = document.querySelector('.header');

export { listMenu, listItems, menuContents, header };
